name: Temporary tactical items integration

on:
  push:
    branches: [main]
    paths:
      - '.github/scripts/temp_tactical_items_patch.py'
      - '.github/scripts/temp_tactical_items_trigger.txt'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply physical tactical gear pass
        run: python3 .github/scripts/temp_tactical_items_patch.py
      - name: Commit game/docs changes only
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -f .github/scripts/temp_tactical_items_patch.py .github/scripts/temp_tactical_items_trigger.txt
          git add -A
          git commit -m "Put gear physically on tactical boards"
          git push origin HEAD:main
