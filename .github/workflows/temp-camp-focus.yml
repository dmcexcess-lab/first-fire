name: Temporary camp focus patch

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/temp-camp-focus.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply approved camp focus pass
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"missing patch anchor in {path}: {old[:90]!r}")
              p.write_text(text.replace(old, new, 1))

          # Early-game runway: three full daily rations for the founder.
          replace_once('game/scripts/FFData.gd', '    "Cooked Food": 2,', '    "Cooked Food": 3,')
          replace_once('game/scripts/FFData.gd', '    "Clean Water": 2,', '    "Clean Water": 3,')

          # Productive chores only become valid when the camp actually needs them.
          replace_once('game/scripts/GameSleepVirus.gd',
          '''func survivor_can_assign(survivor) -> bool:\n''',
          '''func camp_chore_needed(chore: String) -> bool:\n    match chore:\n        "stoke_fire": return fire_level <= 48.0\n        "clean_camp": return camp_maintenance <= 68.0\n        "repair_perimeter": return camp_maintenance <= 42.0\n    return false\n\nfunc start_camp_chore(sid: int, chore: String) -> bool:\n    if not camp_chore_needed(chore):\n        toast_requested.emit("That part of camp does not need attention yet.")\n        return false\n    return super.start_camp_chore(sid, chore)\n\nfunc survivor_can_assign(survivor) -> bool:\n''')

          # Camp tutorial and layout polish.
          replace_once('game/scripts/MainSleepVirus.gd',
          '"body": "Watch First Fire directly. Tap survivors for stats and gear, the stash for communal inventory, structures for their work, empty plots to build, and the gate to send someone out."',
          '"body": "Watch First Fire directly. Drag the camp to look around. Tap survivors for stats and gear, the stash for communal inventory, structures for their work, empty plots to build, and the gate to leave camp."')
          replace_once('game/scripts/MainSleepVirus.gd',
          '"body": "Tap the work board for chores and pet care. Tap empty plots to build. Sleeping, eating, drinking, fun, recovery, and social downtime happen through the living camp without menu micromanagement."',
          '"body": "The work board stays quiet until something actually needs attention. Tap it when an alert appears. Sleeping, eating, drinking, fun, recovery, and social downtime happen on their own."')
          replace_once('game/scripts/MainSleepVirus.gd',
          '"body": "Tap the camp gate to choose an available survivor and SEND OUT. Outside camp, tactical movement, darkness, sound, wounds, infection, loot, rescue and extraction determine what comes home."',
          '"body": "Tap the gate to choose a survivor and destination in one place, then LEAVE CAMP. Outside, tactical movement, darkness, sound, wounds, infection, loot, rescue and extraction determine what comes home."')
          replace_once('game/scripts/MainSleepVirus.gd',
          '''var camp_context_kind := ""\nvar camp_context_target := ""\n''',
          '''var camp_context_kind := ""\nvar camp_context_target := ""\nvar gate_survivor_ids: Array = []\nvar gate_survivor_index := 0\nvar gate_zone_names: Array = []\nvar gate_zone_index := 0\n''')
          replace_once('game/scripts/MainSleepVirus.gd',
          '''    _hide_legacy_navigation()\n    _apply_camp_menu_layout()\n''',
          '''    _hide_legacy_navigation()\n    _hide_development_placeholder()\n    _apply_camp_menu_layout()\n''')
          replace_once('game/scripts/MainSleepVirus.gd',
          '''func _build_inspector_overlay():\n''',
          '''func _hide_development_placeholder() -> void:\n    for node in find_children("*", "Label", true, false):\n        if node is Label and str(node.text).begins_with("AD SPACE"):\n            var panel := node.get_parent()\n            if panel is Control:\n                panel.visible = false\n                panel.custom_minimum_size = Vector2.ZERO\n            return\n\nfunc _build_inspector_overlay():\n''')
          replace_once('game/scripts/MainSleepVirus.gd',
          '''func _apply_camp_menu_layout() -> void:\n    if camp_view == null or content_scroll == null:\n        return\n    current_tab = "Camp"\n    var home := camp_context_kind == ""\n    content_scroll.visible = not home\n    var frame := camp_view.get_parent()\n    if frame != null:\n        frame.size_flags_vertical = Control.SIZE_EXPAND_FILL if home else Control.SIZE_SHRINK_BEGIN\n        frame.custom_minimum_size = Vector2(0, 430 if home else 300)\n    camp_view.custom_minimum_size = Vector2(0, 430 if home else 300)\n    camp_view.set_menu_mode(true)\n    camp_view.queue_redraw()\n''',
          '''func _apply_camp_menu_layout() -> void:\n    if camp_view == null or content_scroll == null:\n        return\n    current_tab = "Camp"\n    var home := camp_context_kind == ""\n    content_scroll.visible = not home\n    var frame := camp_view.get_parent()\n    if frame != null:\n        frame.size_flags_vertical = Control.SIZE_EXPAND_FILL if home else Control.SIZE_SHRINK_BEGIN\n        frame.custom_minimum_size = Vector2(0, 520 if home else 260)\n    camp_view.custom_minimum_size = Vector2(0, 520 if home else 260)\n    camp_view.set_menu_mode(true)\n    camp_view.queue_redraw()\n''')
          replace_once('game/scripts/MainSleepVirus.gd',
          '''func _on_camp_gate_pressed() -> void:\n    _open_camp_context("gate")\n''',
          '''func _on_camp_gate_pressed() -> void:\n    _prepare_gate_context(selected_survivor_id)\n    _open_camp_context("gate")\n''')

          old_gate = '''func _draw_gate_context() -> void:\n    _draw_context_header("CAMP GATE", "Choose who leaves First Fire. Busy, sleeping, sick, quarantined or recovering survivors cannot be sent out.")\n    for survivor_value in Game.survivors:\n        var survivor: Dictionary = survivor_value\n        if str(survivor.get("condition", "Dead")) == "Dead":\n            continue\n        var panel := PanelContainer.new()\n        var v := VBoxContainer.new()\n        panel.add_child(v)\n        v.add_child(_make_label("%s — %s" % [str(survivor.get("name", "Survivor")), _activity_text(survivor)], 15))\n        var equipment: Dictionary = survivor.get("equipment", {})\n        v.add_child(_make_label("%s  •  %s  •  Fatigue %.0f  Stress %.0f" % [str(equipment.get("Weapon", "Unarmed")), str(equipment.get("Pack", "No pack")), float(survivor.get("fatigue", 0.0)), float(survivor.get("stress", 0.0))], 11))\n        var row := HBoxContainer.new()\n        var inspect := Button.new()\n        inspect.text = "INSPECT"\n        inspect.custom_minimum_size = Vector2(0, 42)\n        inspect.size_flags_horizontal = Control.SIZE_EXPAND_FILL\n        inspect.pressed.connect(_open_survivor_inspector.bind(int(survivor.get("id", -1))))\n        row.add_child(inspect)\n        var send := Button.new()\n        send.text = "SEND OUT"\n        send.custom_minimum_size = Vector2(0, 42)\n        send.size_flags_horizontal = Control.SIZE_EXPAND_FILL\n        send.disabled = not Game.survivor_can_assign(survivor)\n        send.pressed.connect(_send_survivor_from_panel.bind(int(survivor.get("id", -1))))\n        row.add_child(send)\n        v.add_child(row)\n        content_box.add_child(panel)\n\n'''
          new_gate = '''func _prepare_gate_context(preferred_id: int = -1) -> void:\n    gate_survivor_ids.clear()\n    for survivor_value in Game.survivors:\n        var survivor: Dictionary = survivor_value\n        if Game.survivor_can_assign(survivor):\n            gate_survivor_ids.append(int(survivor.get("id", -1)))\n    gate_survivor_index = 0\n    if preferred_id >= 0 and gate_survivor_ids.has(preferred_id):\n        gate_survivor_index = gate_survivor_ids.find(preferred_id)\n    gate_zone_names.clear()\n    for zone_value in CampData.ZONE_ORDER:\n        var zone := str(zone_value)\n        if Game.unlocked_zones.has(zone):\n            gate_zone_names.append(zone)\n    gate_zone_index = 0\n\nfunc _draw_gate_context() -> void:\n    _draw_context_header("CAMP GATE", "Choose who goes and where. This is the whole send-out flow.")\n    if gate_survivor_ids.is_empty():\n        content_box.add_child(_make_label("No survivor is currently free to leave camp.", 13))\n        return\n    gate_survivor_index = clampi(gate_survivor_index, 0, gate_survivor_ids.size() - 1)\n    var survivor_id := int(gate_survivor_ids[gate_survivor_index])\n    var survivor: Variant = Game.get_survivor(survivor_id)\n    if survivor == null:\n        content_box.add_child(_make_label("That survivor is no longer available.", 13))\n        return\n    selected_survivor_id = survivor_id\n    var survivor_row := HBoxContainer.new()\n    if gate_survivor_ids.size() > 1:\n        var prev := Button.new()\n        prev.text = "PREV"\n        prev.custom_minimum_size = Vector2(64, 44)\n        prev.pressed.connect(_cycle_gate_survivor.bind(-1))\n        survivor_row.add_child(prev)\n    var equipment: Dictionary = survivor.get("equipment", {})\n    var survivor_text := "%s\\n%s • %s • Fatigue %.0f • Stress %.0f" % [str(survivor.get("name", "Survivor")), str(equipment.get("Weapon", "Unarmed")), str(equipment.get("Pack", "No pack")), float(survivor.get("fatigue", 0.0)), float(survivor.get("stress", 0.0))]\n    var survivor_label := _make_label(survivor_text, 12)\n    survivor_label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER\n    survivor_label.size_flags_horizontal = Control.SIZE_EXPAND_FILL\n    survivor_row.add_child(survivor_label)\n    if gate_survivor_ids.size() > 1:\n        var next := Button.new()\n        next.text = "NEXT"\n        next.custom_minimum_size = Vector2(64, 44)\n        next.pressed.connect(_cycle_gate_survivor.bind(1))\n        survivor_row.add_child(next)\n    content_box.add_child(survivor_row)\n    var inspect := Button.new()\n    inspect.text = "INSPECT %s" % str(survivor.get("name", "SURVIVOR")).get_slice(" ", 0).to_upper()\n    inspect.custom_minimum_size = Vector2(0, 40)\n    inspect.pressed.connect(_open_survivor_inspector.bind(survivor_id))\n    content_box.add_child(inspect)\n\n    content_box.add_child(_separator())\n    if gate_zone_names.is_empty():\n        content_box.add_child(_make_label("No expedition zone is currently unlocked.", 13))\n        return\n    gate_zone_index = clampi(gate_zone_index, 0, gate_zone_names.size() - 1)\n    var zone := str(gate_zone_names[gate_zone_index])\n    var zone_data: Dictionary = CampData.ZONES.get(zone, {})\n    var zone_row := HBoxContainer.new()\n    if gate_zone_names.size() > 1:\n        var zone_prev := Button.new()\n        zone_prev.text = "PREV"\n        zone_prev.custom_minimum_size = Vector2(64, 44)\n        zone_prev.pressed.connect(_cycle_gate_zone.bind(-1))\n        zone_row.add_child(zone_prev)\n    var zone_label := _make_label(zone, 15)\n    zone_label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER\n    zone_label.vertical_alignment = VERTICAL_ALIGNMENT_CENTER\n    zone_label.size_flags_horizontal = Control.SIZE_EXPAND_FILL\n    zone_row.add_child(zone_label)\n    if gate_zone_names.size() > 1:\n        var zone_next := Button.new()\n        zone_next.text = "NEXT"\n        zone_next.custom_minimum_size = Vector2(64, 44)\n        zone_next.pressed.connect(_cycle_gate_zone.bind(1))\n        zone_row.add_child(zone_next)\n    content_box.add_child(zone_row)\n    content_box.add_child(_make_label("%.0fs • %s • %s" % [float(zone_data.get("duration", 0.0)), str(zone_data.get("danger", "?")), Game.zone_loot_state(zone)], 12))\n    content_box.add_child(_make_label("Likely finds: %s" % _zone_focus_text(zone), 12))\n    var leave := Button.new()\n    leave.text = "LEAVE CAMP"\n    leave.custom_minimum_size = Vector2(0, 48)\n    leave.pressed.connect(_leave_from_gate)\n    content_box.add_child(leave)\n\n    var found_site := false\n    for site_value in Game.special_sites.keys():\n        var site := str(site_value)\n        var site_state: Dictionary = Game.special_sites[site]\n        if not bool(site_state.get("discovered", false)) or bool(site_state.get("cleared", false)):\n            continue\n        if not found_site:\n            content_box.add_child(_separator())\n            content_box.add_child(_make_label("Known special sites", 13))\n            found_site = true\n        var site_button := Button.new()\n        site_button.text = "%s — %.0fs" % [site, float(CampData.SPECIAL_SITES[site].get("duration", 0.0))]\n        site_button.custom_minimum_size = Vector2(0, 42)\n        site_button.pressed.connect(_leave_special_from_gate.bind(site))\n        content_box.add_child(site_button)\n\nfunc _cycle_gate_survivor(direction: int) -> void:\n    if gate_survivor_ids.size() <= 1:\n        return\n    gate_survivor_index = posmod(gate_survivor_index + direction, gate_survivor_ids.size())\n    call_deferred("_refresh_content")\n\nfunc _cycle_gate_zone(direction: int) -> void:\n    if gate_zone_names.size() <= 1:\n        return\n    gate_zone_index = posmod(gate_zone_index + direction, gate_zone_names.size())\n    call_deferred("_refresh_content")\n\nfunc _zone_focus_text(zone: String) -> String:\n    match zone:\n        "Camp Perimeter": return "food, water, wood"\n        "Nearby Streets": return "food, water, mixed materials"\n        "Residential Blocks": return "food, cloth, medicine"\n        "Commercial Fringe": return "hardware, scrap, medicine"\n        "Industrial Edge": return "scrap, hardware, ammo"\n    return "mixed supplies"\n\nfunc _leave_from_gate() -> void:\n    if gate_survivor_ids.is_empty() or gate_zone_names.is_empty():\n        return\n    var survivor_id := int(gate_survivor_ids[clampi(gate_survivor_index, 0, gate_survivor_ids.size() - 1)])\n    var zone := str(gate_zone_names[clampi(gate_zone_index, 0, gate_zone_names.size() - 1)])\n    selected_survivor_id = survivor_id\n    if Game.start_expedition(survivor_id, zone):\n        _close_camp_context()\n\nfunc _leave_special_from_gate(site: String) -> void:\n    if gate_survivor_ids.is_empty():\n        return\n    var survivor_id := int(gate_survivor_ids[clampi(gate_survivor_index, 0, gate_survivor_ids.size() - 1)])\n    selected_survivor_id = survivor_id\n    if Game.start_special_site(survivor_id, site):\n        _close_camp_context()\n\nfunc _send_survivor_from_panel(id):\n    selected_survivor_id = int(id)\n    _prepare_gate_context(selected_survivor_id)\n    _open_camp_context("gate")\n\nfunc _on_inspector_send(id):\n    _send_survivor_from_panel(id)\n\nfunc _worker_picker():\n    var box := VBoxContainer.new()\n    var avail: Array = Game.available_survivors()\n    if avail.is_empty():\n        selected_worker_id = -1\n        box.add_child(_make_label("No available survivor", 13))\n        return box\n    var valid_current := false\n    for survivor_value in avail:\n        if int(survivor_value.get("id", -1)) == selected_worker_id:\n            valid_current = true\n            break\n    if not valid_current:\n        selected_worker_id = int(avail[0].get("id", -1))\n    var current: Variant = Game.get_survivor(selected_worker_id)\n    if avail.size() == 1:\n        box.add_child(_make_label("Worker — %s" % str(current.get("name", "Survivor")), 13))\n        return box\n    var current_index := 0\n    for i in range(avail.size()):\n        if int(avail[i].get("id", -1)) == selected_worker_id:\n            current_index = i\n            break\n    var row := HBoxContainer.new()\n    var prev := Button.new()\n    prev.text = "PREV"\n    prev.custom_minimum_size = Vector2(64, 44)\n    prev.pressed.connect(_on_worker_cycle.bind(-1))\n    row.add_child(prev)\n    var label := _make_label("%s  •  %d/%d" % [str(current.get("name", "Survivor")), current_index + 1, avail.size()], 12)\n    label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER\n    label.vertical_alignment = VERTICAL_ALIGNMENT_CENTER\n    label.size_flags_horizontal = Control.SIZE_EXPAND_FILL\n    row.add_child(label)\n    var next := Button.new()\n    next.text = "NEXT"\n    next.custom_minimum_size = Vector2(64, 44)\n    next.pressed.connect(_on_worker_cycle.bind(1))\n    row.add_child(next)\n    box.add_child(row)\n    return box\n\n'''
          replace_once('game/scripts/MainSleepVirus.gd', old_gate, new_gate)

          replace_once('game/scripts/MainSleepVirus.gd',
          '''    status_label.text = "Day %d  %s  •  Food %d  Water %d  •  Pop %d/%d  Beds %d" % [\n        Game.day,\n        Game.formatted_time(),\n        int(Game.resources.get("Cooked Food", 0)),\n        int(Game.resources.get("Clean Water", 0)),\n        Game.population(),\n        Game.MAX_POPULATION,\n        Game.shelter_capacity(),\n    ]\n''',
          '''    status_label.text = "D%d  %s  •  Food %d  Water %d" % [\n        Game.day,\n        Game.formatted_time(),\n        int(Game.resources.get("Cooked Food", 0)),\n        int(Game.resources.get("Clean Water", 0)),\n    ]\n''')

          board_start = Path('game/scripts/MainSleepVirus.gd').read_text().index('func _draw_camp_work_board() -> void:')
          board_prefix = Path('game/scripts/MainSleepVirus.gd').read_text()[:board_start]
          board_new = '''func _draw_camp_work_board() -> void:\n    content_box.add_child(_separator())\n    content_box.add_child(_heading("Camp Work", 19))\n    for survivor_value in Game.survivors:\n        var survivor: Dictionary = survivor_value\n        if str(survivor.get("status", "")) in ["Chore", "Pet Care"] and not survivor.get("task", {}).is_empty():\n            var task: Dictionary = survivor["task"]\n            var row := HBoxContainer.new()\n            row.size_flags_horizontal = Control.SIZE_EXPAND_FILL\n            row.add_child(_make_label("%s — %s  %d/%d" % [survivor["name"], task.get("label", "Work"), int(task.get("progress", 0)), int(task.get("goal", 1))], 12))\n            var work := Button.new()\n            work.text = "WORK"\n            work.custom_minimum_size = Vector2(92, 44)\n            work.pressed.connect(Game.perform_camp_task_tap.bind(int(survivor["id"])))\n            row.add_child(work)\n            content_box.add_child(row)\n\n    var needed: Array = []\n    if Game.camp_chore_needed("stoke_fire"):\n        needed.append(["STOKE FIRE", "stoke_fire", int(Game.resources.get("Wood", 0)) > 0, "needs 1 Wood"])\n    if Game.camp_chore_needed("clean_camp"):\n        needed.append(["CLEAN CAMP", "clean_camp", true, ""])\n    if Game.camp_chore_needed("repair_perimeter"):\n        var repair_material := int(Game.resources.get("Scrap Metal", 0)) > 0 or int(Game.resources.get("Wood", 0)) > 0\n        needed.append(["REPAIR PERIMETER", "repair_perimeter", repair_material, "needs Scrap Metal or Wood"])\n\n    if needed.is_empty():\n        content_box.add_child(_make_label("Nothing urgent right now. Watch the camp; the board will call for work when something actually needs attention.", 12))\n    else:\n        content_box.add_child(_worker_picker())\n        for entry_value in needed:\n            var entry: Array = entry_value\n            var button := Button.new()\n            button.text = str(entry[0]) + ("" if bool(entry[2]) else " — " + str(entry[3]))\n            button.custom_minimum_size = Vector2(0, 44)\n            button.disabled = selected_worker_id < 0 or not bool(entry[2])\n            button.pressed.connect(Game.start_camp_chore.bind(selected_worker_id, str(entry[1])))\n            content_box.add_child(button)\n    content_box.add_child(_make_label("Fire %.0f%%  •  Maintenance %.0f%%" % [Game.fire_level, Game.camp_maintenance], 12))\n\n    content_box.add_child(_separator())\n    content_box.add_child(_heading("Pets", 19))\n    if Game.pets.is_empty():\n        content_box.add_child(_make_label("No camp pets yet. Some tactical rescue calls may be a stranded dog or cat.", 12))\n        return\n    content_box.add_child(_worker_picker())\n    for pet_value in Game.pets:\n        var pet: Dictionary = pet_value\n        var panel := PanelContainer.new()\n        var v := VBoxContainer.new()\n        panel.add_child(v)\n        v.add_child(_make_label("%s — %s — %s" % [pet.get("name", "Pet"), pet.get("species", "Animal"), Game.pet_mood_label(pet)], 16))\n        var needs: Dictionary = Game.CampLifeRules.normalize_pet_needs(pet.get("needs", {}))\n        v.add_child(_make_label("Bond %.0f  •  Play with and love them or they may leave camp." % needs["affection"], 12))\n        v.add_child(_make_label("Each retained pet finds 1 material or Raw Food per day.", 11))\n        var buttons := GridContainer.new()\n        buttons.columns = 2\n        for action in ["play", "love"]:\n            var button := Button.new()\n            button.text = action.to_upper()\n            button.custom_minimum_size = Vector2(0, 42)\n            button.disabled = selected_worker_id < 0\n            button.pressed.connect(Game.start_pet_care.bind(selected_worker_id, int(pet["id"]), action))\n            buttons.add_child(button)\n        v.add_child(buttons)\n        content_box.add_child(panel)\n'''
          Path('game/scripts/MainSleepVirus.gd').write_text(board_prefix + board_new)

          # Zoomed, pannable camp and quieter affordances.
          replace_once('game/scripts/FFCampViewSleepVirus.gd', 'const MENU_VISIBLE_GRID_WIDTH := 15.5', 'const MENU_VISIBLE_GRID_WIDTH := 8.5\nconst PAN_DRAG_THRESHOLD := 8.0')
          replace_once('game/scripts/FFCampViewSleepVirus.gd',
          '''var menu_mode := false\n''',
          '''var menu_mode := false\nvar menu_pan := Vector2.ZERO\nvar drag_active := false\nvar drag_start := Vector2.ZERO\nvar drag_pan_start := Vector2.ZERO\nvar drag_moved := false\n''')
          replace_once('game/scripts/FFCampViewSleepVirus.gd',
          '''func _camp_geometry() -> Dictionary:\n    var visible_width := MENU_VISIBLE_GRID_WIDTH if menu_mode else float(GRID_W)\n    var tile: float = minf(size.x / visible_width, size.y / float(GRID_H))\n    var map_size := Vector2(tile * float(GRID_W), tile * float(GRID_H))\n    var origin := (size - map_size) * 0.5\n    return {"tile": tile, "origin": origin, "map_size": map_size}\n''',
          '''func _camp_geometry() -> Dictionary:\n    if not menu_mode:\n        var base_tile: float = minf(size.x / float(GRID_W), size.y / float(GRID_H))\n        var base_map_size := Vector2(base_tile * float(GRID_W), base_tile * float(GRID_H))\n        return {"tile": base_tile, "origin": (size - base_map_size) * 0.5, "map_size": base_map_size}\n    var tile: float = size.x / MENU_VISIBLE_GRID_WIDTH\n    var map_size := Vector2(tile * float(GRID_W), tile * float(GRID_H))\n    var base_origin := (size - map_size) * 0.5\n    var origin := Vector2(\n        _panned_axis_origin(size.x, map_size.x, base_origin.x, menu_pan.x),\n        _panned_axis_origin(size.y, map_size.y, base_origin.y, menu_pan.y)\n    )\n    return {"tile": tile, "origin": origin, "map_size": map_size}\n\nfunc _panned_axis_origin(view_size: float, map_size: float, base_origin: float, pan: float) -> float:\n    if map_size <= view_size:\n        return (view_size - map_size) * 0.5\n    return clampf(base_origin + pan, view_size - map_size, 0.0)\n\nfunc _set_menu_pan(value: Vector2) -> void:\n    var tile: float = size.x / MENU_VISIBLE_GRID_WIDTH\n    var map_size := Vector2(tile * float(GRID_W), tile * float(GRID_H))\n    var base_origin := (size - map_size) * 0.5\n    var next := value\n    if map_size.x <= size.x:\n        next.x = 0.0\n    else:\n        next.x = clampf(next.x, size.x - map_size.x - base_origin.x, -base_origin.x)\n    if map_size.y <= size.y:\n        next.y = 0.0\n    else:\n        next.y = clampf(next.y, size.y - map_size.y - base_origin.y, -base_origin.y)\n    menu_pan = next\n    queue_redraw()\n''')

          source = Path('game/scripts/FFCampViewSleepVirus.gd').read_text()
          a = source.index('func _draw_build_plots(origin: Vector2, tile: float) -> void:')
          b = source.index('func _draw_communal_chest(origin: Vector2, tile: float) -> void:')
          source = source[:a] + '''func _draw_build_plots(origin: Vector2, tile: float) -> void:\n    if not menu_mode:\n        return\n    for building_value in BUILDING_CELLS.keys():\n        var building_name := str(building_value)\n        if bool(Game.buildings.get(building_name, false)):\n            continue\n        var cell: Vector2i = BUILDING_CELLS[building_value]\n        var rect := _cell_rect(cell, origin, tile).grow(-tile * 0.14)\n        draw_rect(rect, Color(0.78, 0.70, 0.45, 0.04))\n        draw_rect(rect, Color(0.82, 0.74, 0.48, 0.32), false, maxf(1.0, tile * 0.025))\n        draw_line(rect.position + Vector2(rect.size.x * 0.34, rect.size.y * 0.50), rect.position + Vector2(rect.size.x * 0.66, rect.size.y * 0.50), Color(0.93, 0.84, 0.58, 0.58), maxf(1.0, tile * 0.04))\n        draw_line(rect.position + Vector2(rect.size.x * 0.50, rect.size.y * 0.34), rect.position + Vector2(rect.size.x * 0.50, rect.size.y * 0.66), Color(0.93, 0.84, 0.58, 0.58), maxf(1.0, tile * 0.04))\n\n''' + source[b:]
          Path('game/scripts/FFCampViewSleepVirus.gd').write_text(source)

          source = Path('game/scripts/FFCampViewSleepVirus.gd').read_text()
          a = source.index('func _draw_menu_affordances(origin: Vector2, tile: float) -> void:')
          b = source.index('func _gui_input(event: InputEvent) -> void:')
          source = source[:a] + '''func _draw_menu_affordances(origin: Vector2, tile: float) -> void:\n    _draw_interaction_ring(_cell_center(CAMP_CHEST_CELL, origin, tile), tile * 0.42)\n    _draw_interaction_ring(_cell_center(FIRE_CELL, origin, tile), tile * 0.48)\n    _draw_interaction_ring(_cell_center(WORK_BOARD_CELL, origin, tile), tile * 0.42)\n    if bool(Game.buildings.get("Workbench", false)):\n        _draw_interaction_ring(_cell_center(building_cell("Workbench"), origin, tile), tile * 0.42)\n    if bool(Game.buildings.get("Sewing Table", false)):\n        _draw_interaction_ring(_cell_center(building_cell("Sewing Table"), origin, tile), tile * 0.42)\n    var gate_center := (_cell_center(Vector2i(7, GRID_H - 1), origin, tile) + _cell_center(Vector2i(8, GRID_H - 1), origin, tile)) * 0.5\n    _draw_interaction_ring(gate_center, tile * 0.55)\n\n    if Game.camp_chore_needed("stoke_fire"):\n        _draw_alert_badge(_cell_center(FIRE_CELL, origin, tile) + Vector2(tile * 0.42, -tile * 0.38), tile)\n    var clean_needed := Game.camp_chore_needed("clean_camp")\n    var repair_needed := Game.camp_chore_needed("repair_perimeter")\n    if clean_needed or repair_needed:\n        _draw_alert_badge(_cell_center(WORK_BOARD_CELL, origin, tile) + Vector2(tile * 0.38, -tile * 0.38), tile)\n    if clean_needed:\n        for dirt_cell in [Vector2i(6, 8), Vector2i(9, 9), Vector2i(11, 8)]:\n            var dirt_center := _cell_center(dirt_cell, origin, tile)\n            draw_circle(dirt_center, tile * 0.09, Color(0.33, 0.24, 0.14, 0.48))\n            draw_circle(dirt_center + Vector2(tile * 0.12, tile * 0.05), tile * 0.05, Color(0.25, 0.19, 0.12, 0.42))\n    if repair_needed:\n        var fence_rect := _cell_rect(Vector2i(16, 5), origin, tile).grow(-tile * 0.10)\n        draw_line(fence_rect.position + Vector2(0, fence_rect.size.y * 0.25), fence_rect.end - Vector2(0, fence_rect.size.y * 0.25), Color(0.92, 0.56, 0.34, 0.92), maxf(2.0, tile * 0.06))\n        draw_line(fence_rect.position + Vector2(fence_rect.size.x, fence_rect.size.y * 0.25), fence_rect.position + Vector2(0, fence_rect.size.y * 0.75), Color(0.92, 0.56, 0.34, 0.92), maxf(2.0, tile * 0.06))\n\nfunc _draw_interaction_ring(center: Vector2, radius: float) -> void:\n    draw_arc(center, radius, 0.0, TAU, 22, Color(0.83, 0.73, 0.46, 0.42), maxf(1.0, radius * 0.05))\n\nfunc _draw_alert_badge(center: Vector2, tile: float) -> void:\n    var radius := maxf(8.0, tile * 0.18)\n    draw_circle(center, radius, Color(0.70, 0.24, 0.18, 0.94))\n    draw_arc(center, radius, 0.0, TAU, 18, Color(0.98, 0.82, 0.45, 0.95), 1.0)\n    var font := get_theme_default_font()\n    var font_size := maxi(9, int(tile * 0.26))\n    draw_string(font, center + Vector2(-radius, float(font_size) * 0.36), "!", HORIZONTAL_ALIGNMENT_CENTER, radius * 2.0, font_size, Color.WHITE)\n\n''' + source[b:]
          Path('game/scripts/FFCampViewSleepVirus.gd').write_text(source)

          source = Path('game/scripts/FFCampViewSleepVirus.gd').read_text()
          a = source.index('func _gui_input(event: InputEvent) -> void:')
          b = source.index('func _handle_camp_press(local_pos: Vector2) -> void:')
          source = source[:a] + '''func _gui_input(event: InputEvent) -> void:\n    if not menu_mode:\n        return\n    if event is InputEventScreenTouch:\n        if event.pressed:\n            _begin_pan_drag(event.position)\n        else:\n            _finish_pan_drag(event.position)\n        accept_event()\n        return\n    if event is InputEventScreenDrag and drag_active:\n        _continue_pan_drag(event.position)\n        accept_event()\n        return\n    if event is InputEventMouseButton and event.button_index == MOUSE_BUTTON_LEFT:\n        if event.pressed:\n            _begin_pan_drag(event.position)\n        else:\n            _finish_pan_drag(event.position)\n        accept_event()\n        return\n    if event is InputEventMouseMotion and drag_active:\n        _continue_pan_drag(event.position)\n        accept_event()\n\nfunc _begin_pan_drag(position: Vector2) -> void:\n    drag_active = true\n    drag_start = position\n    drag_pan_start = menu_pan\n    drag_moved = false\n\nfunc _continue_pan_drag(position: Vector2) -> void:\n    if not drag_active:\n        return\n    var delta := position - drag_start\n    if not drag_moved and delta.length() >= PAN_DRAG_THRESHOLD:\n        drag_moved = true\n    if drag_moved:\n        _set_menu_pan(drag_pan_start + delta)\n\nfunc _finish_pan_drag(position: Vector2) -> void:\n    if not drag_active:\n        return\n    if not drag_moved:\n        _handle_camp_press(position)\n    drag_active = false\n    drag_moved = false\n\n''' + source[b:]
          Path('game/scripts/FFCampViewSleepVirus.gd').write_text(source)

          # Durable source contracts for the approved camp-focus behavior.
          replace_once('game/scripts/ci/FFArchitectureSmoke.gd',
          '''    if not _check(active_camp_source.contains("status == \\"Sleeping\\"") and active_camp_source.contains("_sleep_cell_for_survivor") and active_camp_source.contains("QUARANTINE"), "camp reflects sleep and isolation"): return\n''',
          '''    if not _check(active_camp_source.contains("status == \\"Sleeping\\"") and active_camp_source.contains("_sleep_cell_for_survivor") and active_camp_source.contains("QUARANTINE"), "camp reflects sleep and isolation"): return\n    if not _check(active_game_source.contains("func camp_chore_needed") and active_main_source.contains("LEAVE CAMP") and active_main_source.contains("Nothing urgent right now") and active_camp_source.contains("MENU_VISIBLE_GRID_WIDTH := 8.5") and active_camp_source.contains("_continue_pan_drag"), "camp focus routing and problem-driven work"): return\n''')
          replace_once('game/scripts/ci/FFArchitectureSmoke.gd',
          '''    var base_needs := CampLifeRules.default_needs()\n''',
          '''    if not _check(int(D.STARTING_RESOURCES.get("Cooked Food", 0)) >= 3 and int(D.STARTING_RESOURCES.get("Clean Water", 0)) >= 3, "new game basic supply runway"): return\n    var base_needs := CampLifeRules.default_needs()\n''')

          # Durable documentation.
          replace_once('README_CONTEXT.md',
          'The **living camp is the primary game/menu surface**. The old CAMP / CRAFT / BUILD / SURVIVORS tab bar is being retired from active play rather than preserved as parallel navigation. All normal management must be reachable by touching physical camp entities and opening contextual sheets/overlays from them.',
          'The **living camp is the primary game/menu surface**. The old CAMP / CRAFT / BUILD / SURVIVORS tab bar is retired from active play rather than preserved as parallel navigation. The phone view is intentionally zoomed and pannable instead of shrinking the whole settlement into a minimap; normal management is reached by touching physical camp entities and opening contextual sheets/overlays from them.')
          replace_once('README_CONTEXT.md',
          'The **camp gate** is the normal route into expedition preparation. Tapping it opens a contextual chooser of living survivors, showing availability and key field readiness; a survivor can then be sent through the existing expedition modal. Tapping a survivor directly still opens their inspector, which also exposes SEND OUT when valid. The old standalone Survivors navigation is hidden from active play.',
          'The **camp gate** owns expedition preparation end-to-end. Tapping it opens one contextual surface for the available survivor, destination, danger, likely resource categories, and LEAVE CAMP action; the old second SEND OUT modal is no longer part of the active camp flow. Tapping a survivor directly still opens their inspector, whose SEND OUT handoff returns to this same gate context. The old standalone Survivors navigation is hidden from active play.')
          replace_once('README_CONTEXT.md',
          'Productive work is player-directed: fire tending, cleaning, perimeter repair, crafting, building, garden work, pet care, and expeditions require assignment. Camp chores and pet care use short touch-first WORK interactions.',
          'Productive work is player-directed: fire tending, cleaning, perimeter repair, crafting, building, garden work, pet care, and expeditions require assignment. Routine chore buttons stay quiet until the fire or maintenance actually crosses an attention threshold; visible dirt/damage/alert cues in camp explain why work is needed before assignment. Camp chores and pet care use short touch-first WORK interactions.')
          replace_once('README_CONTEXT.md',
          'Fire Pit conversions:\n- **1 Raw Food → 2 Cooked Food**\n- **1 Dirty Water → 2 Clean Water**',
          'New games begin with three Cooked Food and three Clean Water so the founder has roughly three daily rations before shortages; scavenging still has to sustain the camp after that.\n\nFire Pit conversions:\n- **1 Raw Food → 2 Cooked Food**\n- **1 Dirty Water → 2 Clean Water**')

          replace_once('ARCHITECTURE.md',
          '`FFCampViewSleepVirus.gd` is the active camp/menu surface: it maps authoritative sleep/treatment/quarantine state, draws at-a-glance mood/need information, performs touch hit-testing against visible camp entities, and emits navigation/selection intent without mutating simulation state.',
          '`FFCampViewSleepVirus.gd` is the active camp/menu surface: it maps authoritative sleep/treatment/quarantine state, draws at-a-glance mood/need information, provides the zoomed/pannable phone viewport plus problem-state cues, performs touch hit-testing against visible camp entities, and emits navigation/selection intent without mutating simulation state.')
          replace_once('ARCHITECTURE.md',
          'Camp objects open narrow contextual sheets: First Fire → starter Fire Pit crafting, Workbench/Sewing Table → station-specific crafting, empty plot → that plot\'s construction project, built structure → contextual details/actions, work board → chores/pets, gate → send-out chooser, survivor → inspector, communal stash → communal inventory.',
          'Camp objects open narrow contextual sheets: First Fire → starter Fire Pit crafting, Workbench/Sewing Table → station-specific crafting, empty plot → that plot\'s construction project, built structure → contextual details/actions, work board → chores/pets only when attention is warranted, gate → survivor + destination + LEAVE CAMP in one sheet, survivor → inspector, communal stash → communal inventory.')

          replace_once('ROADMAP.md',
          'The living camp is the primary home/menu surface. The old CAMP / CRAFT / BUILD / SURVIVORS tab bar is being retired from active play rather than preserved as parallel navigation.',
          'The living camp is the primary home/menu surface. The old CAMP / CRAFT / BUILD / SURVIVORS tab bar is retired from active play rather than preserved as parallel navigation. On phones the camp is zoomed and pannable so structures and people remain readable instead of shrinking the full 18×11 settlement into one strip.')
          replace_once('ROADMAP.md',
          '- an active camp work loop covering fire tending, cleaning, perimeter maintenance, building, crafting, gardening, pet care and expeditions; chores are assigned from the camp and short active WORK interactions remain available where appropriate;',
          '- an active camp work loop covering fire tending, cleaning, perimeter maintenance, building, crafting, gardening, pet care and expeditions; routine chores only surface after visible fire/maintenance problems create a reason to act, and short active WORK interactions remain available where appropriate;')

          changelog = Path('CHANGELOG.md').read_text()
          entry = '''## Beta Candidate — Camp Focus & Problem-Driven Management — 2026-09-14\n\n- Zoomed the living camp for portrait play and added drag panning instead of shrinking the full settlement into a narrow minimap strip.\n- Removed persistent BUILD/CRAFT/STASH/WORK/SEND OUT text clutter from the camp; physical objects remain tappable and subtle interaction rings plus problem alerts carry the affordance.\n- Camp chores are now problem-driven: fire tending appears below 48%, cleaning below 68% maintenance, and perimeter repair below 42%. The camp view adds visible dirt/damage/alert cues when attention is actually needed.\n- Flattened expedition preparation into the physical gate sheet: choose the available survivor, destination, see danger/likely finds, then LEAVE CAMP without entering a second SEND OUT modal. Inspector SEND OUT routes back to the same gate flow.\n- Simplified one-survivor worker selection so disabled PREV/NEXT controls no longer clutter early camp tasks.\n- Removed the development ad placeholder from active gameplay and compacted the top camp HUD for narrow phones.\n- New games now start with 3 Cooked Food and 3 Clean Water, giving the founder roughly three daily rations before scavenging must sustain the settlement.\n\n'''
          Path('CHANGELOG.md').write_text(entry + changelog)
          PY

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add game/scripts/FFData.gd game/scripts/GameSleepVirus.gd game/scripts/MainSleepVirus.gd game/scripts/FFCampViewSleepVirus.gd game/scripts/ci/FFArchitectureSmoke.gd README_CONTEXT.md ARCHITECTURE.md ROADMAP.md CHANGELOG.md
          git diff --cached --check
          git commit -m "Focus living camp management"
          git push
