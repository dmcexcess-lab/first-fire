--- scripts/Game.gd
+++ scripts/Game.gd
@@ -7,6 +7,13 @@
 signal toast_requested(message)
 
 const D = preload("res://scripts/FFData.gd")
+const ExpeditionRules = preload("res://scripts/FFExpeditionRules.gd")
+const TacticalScenarios = preload("res://scripts/FFTacticalScenarios.gd")
+const LegacyFieldEvents = preload("res://scripts/FFFieldEventsLegacy.gd")
+const SaveCodec = preload("res://scripts/FFSaveCodec.gd")
+const CampLifeRules = preload("res://scripts/FFCampLifeRules.gd")
+const CampSocial = preload("res://scripts/FFCampSocial.gd")
+# Legacy filename is intentionally preserved so this behavior-only refactor does not reset Alpha saves.
 const SAVE_PATH := "user://first_fire_alpha01.json"
 const SAVE_SCHEMA_VERSION := 3
 const DAY_SECONDS := 120.0
@@ -54,7 +61,7 @@
 
 func _ready():
     rng.randomize()
-    save_existed_on_boot = FileAccess.file_exists(SAVE_PATH)
+    save_existed_on_boot = SaveCodec.exists(SAVE_PATH)
     if save_existed_on_boot:
         load_game()
     else:
@@ -63,7 +70,7 @@
     set_process(true)
 
 func has_save_game():
-    return FileAccess.file_exists(SAVE_PATH)
+    return SaveCodec.exists(SAVE_PATH)
 
 func new_game():
     day = 1
@@ -105,7 +112,7 @@
     alpha_complete_shown = false
     recent_expedition_ids = []
     camp_event_accum = 0.0
-    camp_event_cooldown = 20.0
+    camp_event_cooldown = CampLifeRules.NEW_GAME_EVENT_COOLDOWN
     sim_paused = false
     var founder = _generate_survivor(true)
     founder["equipment"] = {"Weapon": "Utility Knife", "Clothing": "", "Pack": "Worn Backpack", "Tool": ""}
@@ -133,8 +140,8 @@
             day_elapsed -= DAY_SECONDS
             _daily_tick()
 
-    if camp_event_accum >= 45.0:
-        camp_event_accum -= 45.0
+    if camp_event_accum >= CampLifeRules.CAMP_EVENT_INTERVAL:
+        camp_event_accum -= CampLifeRules.CAMP_EVENT_INTERVAL
         _consider_camp_event()
 
     _consider_politics()
@@ -315,25 +322,12 @@
     return s
 
 func relationship_label(value):
-    value = int(value)
-    if value >= 60: return "Close"
-    if value >= 25: return "Friendly"
-    if value <= -60: return "Hostile"
-    if value <= -25: return "Tense"
-    return "Neutral"
+    return CampSocial.relationship_label(int(value))
 
 func _change_relationship(a, b, amount):
     if a == null or b == null:
         return
-    var key = str(b["id"])
-    var adjusted = float(amount)
-    if amount > 0 and a["traits"].has("Friendly"):
-        adjusted *= 1.25
-    if amount > 0 and a["traits"].has("Loner"):
-        adjusted *= 0.5
-    if amount < 0 and leader_id == int(a["id"]) and a["leader_ability"] == "Mediator":
-        adjusted *= 0.75
-    a["relationships"][key] = clamp(int(a["relationships"].get(key, 0)) + int(round(adjusted)), -100, 100)
+    CampSocial.change_relationship(a, b, int(amount), leader_id)
 
 func _change_reputation(s, amount):
     s["reputation"] = int(s.get("reputation", 0)) + int(amount)
@@ -377,15 +371,13 @@
 
         # Idle survivors recover automatically while remaining Available for work.
         if s["status"] == "Available":
-            var fatigue_rate = 0.5 if buildings.get("Cabin", false) else (1.0 / 3.0)
-            var stress_rate = (1.0 / 6.0) if buildings.get("Cabin", false) else 0.1
+            var caretaker_leader := false
             if leader_id != -1:
                 var leader: Variant = get_survivor(leader_id)
-                if leader != null and leader["leader_ability"] == "Caretaker":
-                    fatigue_rate *= 1.2
-                    stress_rate *= 1.2
-            s["fatigue"] = max(0.0, float(s["fatigue"]) - fatigue_rate * delta)
-            s["stress"] = max(0.0, float(s["stress"]) - stress_rate * delta)
+                caretaker_leader = leader != null and leader["leader_ability"] == "Caretaker"
+            var recovery := CampLifeRules.idle_recovery_rates(bool(buildings.get("Cabin", false)), caretaker_leader)
+            s["fatigue"] = max(0.0, float(s["fatigue"]) - recovery.x * delta)
+            s["stress"] = max(0.0, float(s["stress"]) - recovery.y * delta)
             if s["condition"] == "Hurt" or s["condition"] == "Wounded":
                 s["injury_remaining"] = max(0.0, float(s["injury_remaining"]) - delta)
                 if s["injury_remaining"] <= 0.0:
@@ -632,15 +624,8 @@
     for sid in party_ids:
         avg_survival += int(get_survivor(sid)["skills"]["Survival"])
     avg_survival /= party_ids.size()
-    var reduction = min(0.20, avg_survival * 0.025)
-    var duration = float(D.ZONES[zone]["duration"]) * (1.0 - reduction)
-    var force_recruit = false
-    if population() == 1 and eligible_expeditions_since_recruit >= 5:
-        force_recruit = true
-    elif population() >= 2 and population() < 5 and eligible_expeditions_since_recruit >= 7:
-        force_recruit = true
-    if population() >= shelter_capacity() + 1 or not recruit_eligible:
-        force_recruit = false
+    var duration = ExpeditionRules.travel_duration(float(D.ZONES[zone]["duration"]), avg_survival)
+    var force_recruit = ExpeditionRules.should_force_recruit(population(), shelter_capacity(), eligible_expeditions_since_recruit, recruit_eligible)
 
     var event_key = ""
     var combat_kind = ""
@@ -666,7 +651,7 @@
     elif event_key == "" and rng.randf() < float(D.ZONES[zone]["event_chance"]):
         # Keep text encounters alive, but let a little over half of random field
         # encounters become playable tactical situations outside the perimeter.
-        if zone != "Camp Perimeter" and rng.randf() < 0.55:
+        if ExpeditionRules.should_use_tactical_event(zone, rng):
             combat_kind = _pick_tactical_kind(zone)
         else:
             event_key = _select_field_event(zone)
@@ -730,36 +715,10 @@
     return true
 
 func _pick_tactical_kind(zone):
-    var roll = rng.randf()
-    if zone == "Nearby Streets":
-        if roll < 0.35: return "rescue"
-        if roll < 0.70: return "explore"
-        return "ambush"
-    if zone == "Residential Blocks":
-        if roll < 0.30: return "rescue"
-        if roll < 0.75: return "explore"
-        return "ambush"
-    if zone == "Commercial Fringe":
-        if roll < 0.20: return "rescue"
-        if roll < 0.70: return "explore"
-        return "ambush"
-    if roll < 0.15: return "rescue"
-    if roll < 0.50: return "explore"
-    return "ambush"
+    return TacticalScenarios.pick_kind(str(zone), rng)
 
 func _combat_location_name(zone, kind):
-    var pool = []
-    if kind == "ambush":
-        pool = ["Narrow Street", "Parking Cut", "Service Alley", "Blocked Intersection"]
-    elif zone == "Nearby Streets":
-        pool = ["Corner Store", "Abandoned Duplex", "Bus Stop Shops", "Detached Garage"]
-    elif zone == "Residential Blocks":
-        pool = ["Ransacked House", "Backyard Workshop", "Vacant Townhouse", "Neighborhood Market"]
-    elif zone == "Commercial Fringe":
-        pool = ["Pharmacy Annex", "Strip-Mall Office", "Loading Dock Store", "Closed Restaurant"]
-    else:
-        pool = ["Machine Shop", "Warehouse Office", "Loading Yard", "Maintenance Building"]
-    return pool[rng.randi_range(0, pool.size() - 1)]
+    return TacticalScenarios.location_name(str(zone), str(kind), rng)
 
 func _combat_condition_hp(s):
     if s == null: return 0
@@ -795,7 +754,7 @@
         "zone": str(exp["zone"]),
         "kind": str(exp.get("combat_kind", "ambush")),
         "location_name": _combat_location_name(str(exp["zone"]), str(exp.get("combat_kind", "ambush"))),
-        "layout": rng.randi_range(0, 2),
+        "layout": TacticalScenarios.layout_index(rng),
         "seed": rng.randi_range(1, 2147483000),
         "runtime": {
             "lead_hp": _combat_condition_hp(lead),
@@ -924,18 +883,9 @@
     state_changed.emit()
 
 func _select_field_event(zone):
-    var pool = []
-    if zone == "Camp Perimeter":
-        pool = ["backpack"]
-    elif zone == "Nearby Streets":
-        pool = ["backpack", "injured_stranger", "dog"]
-    elif zone == "Residential Blocks":
-        pool = ["someone_inside", "injured_stranger", "dog", "locked_garage", "gunshot"]
-    elif zone == "Commercial Fringe":
-        pool = ["injured_stranger", "gunshot", "discover_market", "discover_clinic", "hardware_cage", "patrol_car", "barricade"]
-    elif zone == "Industrial Edge":
-        pool = ["gunshot", "barricade", "construction_trailer", "locked_office"]
-    return pool[rng.randi_range(0, pool.size() - 1)] if not pool.is_empty() else ""
+    # Compatibility facade. Delete with FFFieldEventsLegacy when Alpha 0.3 has
+    # converted every outside-world text encounter to a tactical scenario.
+    return LegacyFieldEvents.select(str(zone), rng)
 
 func _find_expedition(eid) -> Variant:
     for exp in expeditions:
@@ -1158,10 +1108,7 @@
     if best_scav >= 6 and rng.randf() < 0.15:
         target_items += 1
 
-    var zone_cap = {
-        "Camp Perimeter": 3, "Nearby Streets": 4, "Residential Blocks": 5,
-        "Commercial Fringe": 6, "Industrial Edge": 7
-    }[zone]
+    var zone_cap = ExpeditionRules.zone_cap(str(zone))
     target_items = min(target_items, int(zone_cap))
 
     var capacity = 0
@@ -1181,43 +1128,7 @@
     return loot
 
 func _loot_item_target(zone):
-    var r = rng.randf()
-    # Total items per routine expedition. Zero-result runs become less common
-    # as survivors push farther from camp, while maximum hauls rise slowly.
-    if zone == "Camp Perimeter":
-        if r < 0.25: return 0
-        if r < 0.70: return 1
-        if r < 0.95: return 2
-        return 3
-    if zone == "Nearby Streets":
-        if r < 0.15: return 0
-        if r < 0.45: return 1
-        if r < 0.80: return 2
-        if r < 0.95: return 3
-        return 4
-    if zone == "Residential Blocks":
-        if r < 0.08: return 0
-        if r < 0.30: return 1
-        if r < 0.60: return 2
-        if r < 0.85: return 3
-        if r < 0.97: return 4
-        return 5
-    if zone == "Commercial Fringe":
-        if r < 0.04: return 0
-        if r < 0.20: return 1
-        if r < 0.45: return 2
-        if r < 0.70: return 3
-        if r < 0.88: return 4
-        if r < 0.97: return 5
-        return 6
-    if r < 0.02: return 0
-    if r < 0.12: return 1
-    if r < 0.32: return 2
-    if r < 0.57: return 3
-    if r < 0.77: return 4
-    if r < 0.89: return 5
-    if r < 0.97: return 6
-    return 7
+    return ExpeditionRules.loot_item_target(str(zone), rng)
 
 func _weighted_loot_pick(zone, focus):
     var table = D.ZONES[zone]["loot"]
@@ -2378,33 +2289,16 @@
     return null
 
 func _tense_pair():
-    for a in survivors:
-        if a["condition"] == "Dead": continue
-        for b in survivors:
-            if b["condition"] == "Dead" or int(a["id"]) >= int(b["id"]): continue
-            if int(a["relationships"].get(str(b["id"]), 0)) <= -25 or int(b["relationships"].get(str(a["id"]), 0)) <= -25:
-                return [a, b]
-    return []
+    return CampSocial.tense_pair(survivors)
 
 func _high_stress_survivor() -> Variant:
-    for s in survivors:
-        if s["condition"] != "Dead" and (float(s["stress"]) >= 80.0 or float(s["fatigue"]) >= 85.0):
-            return s
-    return null
+    return CampSocial.high_stress_survivor(survivors)
 
 func _highest_stress_survivor() -> Variant:
-    var best: Variant = null
-    for s in survivors:
-        if s["condition"] == "Dead": continue
-        if best == null or float(s["stress"]) > float(best["stress"]): best = s
-    return best
+    return CampSocial.highest_stress_survivor(survivors)
 
 func _highest_skill_survivor(skill) -> Variant:
-    var best: Variant = null
-    for s in survivors:
-        if s["condition"] == "Dead": continue
-        if best == null or int(s["skills"][skill]) > int(best["skills"][skill]): best = s
-    return best
+    return CampSocial.highest_skill_survivor(survivors, str(skill))
 
 func _resolve_sleep_event(event, mode):
     var living = []
@@ -2465,22 +2359,10 @@
         _queue_event(_build_formal_election())
 
 func _candidate_standing(s):
-    var rel_sum = 0
-    var rel_count = 0
-    for other in survivors:
-        if other["condition"] == "Dead" or int(other["id"]) == int(s["id"]): continue
-        rel_sum += int(other["relationships"].get(str(s["id"]), 0))
-        rel_count += 1
-    var avg = float(rel_sum) / rel_count if rel_count > 0 else 0.0
-    return int(s["skills"]["Social"]) * 6 + int(s["reputation"]) + int(clamp(avg * 0.5, -25.0, 25.0))
+    return CampSocial.candidate_standing(s, survivors)
 
 func _top_candidates():
-    var living = []
-    for s in survivors:
-        if s["condition"] != "Dead": living.append(s)
-    living.sort_custom(func(a, b): return _candidate_standing(a) > _candidate_standing(b))
-    if living.size() >= 2: return [living[0], living[1]]
-    return living
+    return CampSocial.top_candidates(survivors)
 
 func _build_coordinator_event():
     var c = _top_candidates()
@@ -2607,7 +2489,7 @@
     if not initialized and survivors.is_empty():
         return
     var data = {
-        "version": "0.1.0",
+        "version": "0.2.0",
         "save_schema": SAVE_SCHEMA_VERSION,
         "day": day, "day_elapsed": day_elapsed,
         "resources": resources, "components": components, "inventory_gear": inventory_gear,
@@ -2624,21 +2506,16 @@
         "game_over": game_over, "alpha_complete": alpha_complete, "alpha_complete_shown": alpha_complete_shown,
         "recent_expedition_ids": recent_expedition_ids,
     }
-    var file = FileAccess.open(SAVE_PATH, FileAccess.WRITE)
-    if file != null:
-        file.store_string(JSON.stringify(data))
+    SaveCodec.write_json(SAVE_PATH, data)
 
 func load_game():
-    var file = FileAccess.open(SAVE_PATH, FileAccess.READ)
-    if file == null:
+    var parsed = SaveCodec.read_json(SAVE_PATH)
+    if parsed == null:
         new_game()
         return
-    var parsed = JSON.parse_string(file.get_as_text())
-    if typeof(parsed) != TYPE_DICTIONARY or int(parsed.get("save_schema", -1)) != SAVE_SCHEMA_VERSION:
+    if not SaveCodec.is_compatible(parsed, SAVE_SCHEMA_VERSION):
         # Alpha development rule: saves are not migrated between schema revisions.
-        # Delete stale data so compatibility code does not accumulate during development.
-        file = null
-        DirAccess.remove_absolute(ProjectSettings.globalize_path(SAVE_PATH))
+        SaveCodec.invalidate(SAVE_PATH)
         save_existed_on_boot = false
         new_game()
         return
